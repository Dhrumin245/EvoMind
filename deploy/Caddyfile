{$EVOMIND_SITE_ADDRESS} {
    encode zstd gzip

    header {
        X-Content-Type-Options "nosniff"
        X-Frame-Options "DENY"
        Referrer-Policy "strict-origin-when-cross-origin"
        -Server
    }

    handle_path /api/* {
        reverse_proxy api:8000
    }

    handle {
        reverse_proxy frontend:3000
    }
}
